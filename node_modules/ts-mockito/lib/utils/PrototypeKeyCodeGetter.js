"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var PrototypeKeyCodeGetter = (function () {
    function PrototypeKeyCodeGetter() {
    }
    PrototypeKeyCodeGetter.prototype.get = function (proto, key) {
        var descriptor = Object.getOwnPropertyDescriptor(proto, key);
        if (descriptor) {
            if (!descriptor.get && !descriptor.set) {
                return proto[key].toString();
            }
            if (descriptor.get && descriptor.set) {
                return descriptor.get.toString() + " " + descriptor.set.toString();
            }
            if (descriptor.get && !descriptor.set) {
                return descriptor.get.toString();
            }
            if (!descriptor.get && descriptor.set) {
                return descriptor.set.toString();
            }
        }
        else {
            return "";
        }
    };
    return PrototypeKeyCodeGetter;
}());
exports.PrototypeKeyCodeGetter = PrototypeKeyCodeGetter;
//# sourceMappingURL=PrototypeKeyCodeGetter.js.map